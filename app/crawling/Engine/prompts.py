"""주식 게시글 분석용 프롬프트 상수"""

# 기본 프롬프트 (현재 사용중)
DEFAULT_PROMPT = """
다음은 주식 게시판에서 크롤링한 텍스트입니다. 이 내용을 분석하여 다음 정보를 추출해주세요:

1. title: 게시글의 제목 (간결하고 핵심적인 제목, 20자 이내)
2. content: 게시글의 핵심 내용 요약 (2-3문장으로 요약)
3. keywords: 이 게시글과 관련된 주요 키워드 (종목명, 테마, 이슈 등 5개 이내)

크롤링된 텍스트:
{text}

출력 형식(JSON):
{{
    "title": "게시글 제목",
    "content": "핵심 내용 요약 (2-3문장)",
    "keywords": ["키워드1", "키워드2", "키워드3", "키워드4", "키워드5"]
}}

규칙:
- title은 반드시 20자 이내로 작성
- content는 2-3문장으로 핵심만 요약
- keywords는 주식 투자와 관련된 핵심 키워드만 추출 (최대 5개)
- 반드시 JSON 형식으로만 응답
"""

# 엄격한 JSON 응답용 프롬프트
STRICT_JSON_PROMPT = """
당신은 주식 관련 게시글을 분석하는 전문가입니다.

다음에 주어질 텍스트(raw_text)는 주식 게시판에서 가져온 데이터입니다.
이 텍스트를 기반으로 아래 세 가지 정보만 생성해주세요:

1. title: 게시글의 핵심 제목
   - 간결하고 핵심적인 제목
   - 20자 이내 (한국어 기준)

2. content: 게시글의 핵심 내용 요약
   - 2~3문장으로 요약
   - 투자 관점에서 중요한 내용 위주로 정리

3. keywords: 이 게시글과 관련된 주요 키워드
   - 종목명, 투자 아이디어, 업종/테마, 이슈 등
   - 무조건 1개 이내
   - 문자열 배열 형태

[분석 대상 raw_text]
{text}

⚠ 중요 규칙:
- 아래 JSON 형식으로만 응답해야 합니다.
- raw_text(원문 내용)를 그대로 다시 출력하지 마세요.
- title, content, keywords만 포함된 JSON만 응답하세요.
- JSON 바깥에 다른 텍스트(설명, 주석 등)를 절대 쓰지 마세요.

출력 형식(JSON 예시):
{{
    "title": "게시글 제목",
    "content": "핵심 내용 요약 (2-3문장)",
    "keywords": ["키워드1"]
}}
"""

# 간단한 요약용 프롬프트
SIMPLE_SUMMARY_PROMPT = """
주식 게시글을 분석하세요.

텍스트:
{text}

JSON으로 응답:
{{"title": "20자 이내 제목", "content": "2-3문장 요약", "keywords": ["키워드"]}}
"""